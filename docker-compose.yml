services:
  postgres:
    image: postgres:16
    container_name: telemetry-postgres
    environment:
      POSTGRES_DB: telemetrydb
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  telemetry-api:
    build:
      context: ./Telemetry.Api/Telemetry.Api
      dockerfile: Dockerfile
    container_name: telemetry-api
    depends_on:
      - postgres
    environment:
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Postgres: Host=postgres;Port=5432;Database=telemetrydb;Username=app;Password=app
    ports:
      - "8080:8080"

volumes:
  pg_data:
